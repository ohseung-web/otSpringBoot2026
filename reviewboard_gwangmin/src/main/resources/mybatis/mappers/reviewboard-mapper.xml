<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="reviewboard.mapper.ReviewBoardMapper">
	
	<insert id="insertBoard" parameterType="ReviewBoardDTO">
		INSERT INTO reviewBoard(title, writer, rating, content) VALUES(#{title}, #{writer}, #{rating}, #{content})
	</insert>
	
	<select id="allSelect" resultType="ReviewBoardDTO">
		SELECT * FROM reviewBoard ORDER BY num DESC LIMIT #{startRow}, #{pageSize}
	</select>
	
	<select id="countBoard" resultType="int">
		SELECT COUNT(*) FROM reviewBoard
	</select>
	
	<select id="ratingAvg" resultType="Double"> 
		SELECT AVG(rating) FROM reviewBoard
	</select>
	
	<select id="oneSelect" resultType="ReviewBoardDTO" parameterType="int">
		SELECT * FROM reviewBoard WHERE num = #{num}
	</select>
	
	<update id="updateBoard" parameterType="ReviewBoardDTO">
		UPDATE reviewBoard SET title = #{title}, rating = #{rating}, content = #{content} WHERE num = #{num}
	</update>
	
	<delete id="deleteBoard" parameterType="int">
		DELETE FROM reviewBoard WHERE num = #{num}
	</delete>
	
	<update id="updateCount" parameterType="int">
		UPDATE reviewBoard SET readCount = readCount + 1 WHERE num = #{num}
	</update>
	
</mapper>